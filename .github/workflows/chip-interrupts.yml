name: Chip interrupt name validation

on:
  push:
    paths:
      - "models/**"
      - "tools/validate_chip_interrupts.py"
  pull_request:
    paths:
      - "models/**"
      - "tools/validate_chip_interrupts.py"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install PyYAML

      - name: Validate chip interrupt names against block models
        run: |
          python tools/validate_chip_interrupts.py \
            --models-root models \
            --docs "models/**/*.yaml"
