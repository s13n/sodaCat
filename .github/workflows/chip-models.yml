name: Chip model validation

on:
  push:
    paths:
      - "schemas/chip.schema.yaml"
      - "models/**"
      - "tools/validate_chips.py"
      - "tools/validate_lib.py"
  pull_request:
    paths:
      - "schemas/chip.schema.yaml"
      - "models/**"
      - "tools/validate_chips.py"
      - "tools/validate_lib.py"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install PyYAML jsonschema

      - name: Validate chip models
        run: |
          python tools/validate_chips.py \
            --schema schemas/chip.schema.yaml \
            --docs "models/**/*.yaml"
